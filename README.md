# @lthn/sdk-typescript-rxjs

RxJS observables, framework-agnostic. Use outside Angular for reactive pipelines.

Auto-generated from the lthn OpenAPI spec at:

  https://github.com/LetheanNetwork/desktop/blob/main/build/sdk/openapi.yaml

Do **not** hand-edit. Regenerate via:

  ```bash
  cd /path/to/desktop
  ./build/sdk/publish.sh typescript-rxjs
  ```

## Install

```bash
npm install @lthn/sdk-typescript-rxjs
```

## License

EUPL-1.2 — same as the upstream lthn project.
